<!DOCTYPE html>
<html>
<body>

<select>
	<% tools.forEach(function(tool){ %>
  <option>  <%=tool.toolName%> </option>
 <% });%>
</select>
  
  <select>
	<% cos.forEach(function(coo){ %>
  <option>  <%=coo.coName%> </option>
 <% });%>
</select>
</body>
</html>
